defaults: &defaults
  host: 127.0.0.1
  port: 27017

development:
  <<: *defaults
  database: finalizer-development

test:
  <<: *defaults
  database: finalizer-test

# set these environment variables on your prod server
production:
  <<: *defaults
  database: finalizer-prod
